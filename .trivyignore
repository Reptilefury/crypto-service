# Ignore system vulnerabilities that can't be fixed in Alpine base image
CVE-2025-46569

# Ignore private key detection (false positive for test keys)
AsymmetricPrivateKey

# Ignore unfixable Node.js vulnerabilities that we've already addressed with overrides
# but still appear in container scan due to nested dependencies
GHSA-vjh7-7g9h-fjfh
CVE-2023-30542
CVE-2025-64756

# Ignore vulnerabilities in development dependencies that don't affect production
CVE-2024-*
GHSA-*-dev
